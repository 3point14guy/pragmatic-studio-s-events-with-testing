<header>
  <h1>Registrations for <%= link_to(@event.name, @event) %></h1>
</header>

<ol>
  <% @registrations.each do |r| %>
    <li>
      <%= mail_to(r.email, r.name) %>
      (<%= r.how_heard %>)
     <%#= link_to "Edit Registration", edit_event_registration_path(@event, r) %>
    </li>

    <% end %>
</ol>
